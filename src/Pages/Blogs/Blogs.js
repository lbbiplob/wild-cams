import React from "react";
import useTitle from "../../useTitle/useTitle";

const Blogs = () => {
  useTitle("Blogs");
  return (
    <div>
      <h2>this is Blogss</h2>
    </div>
  );
};

export default Blogs;
